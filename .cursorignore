# Claude Code hooks — не индексировать и не подмешивать в контекст по умолчанию.
# Напоминания для Cursor: .cursor/rules/iwe-*.mdc. Чтение .claude/hooks/ агентом часто открывает вкладки.
# Чтобы править хуки с агентом: временно удалите или закомментируйте строку ниже.
.claude/hooks/


# Копия исходников хуков для установки в %USERPROFILE%\.iwe\claude-hooks — не индексировать.
share/claude-hooks-dist/

